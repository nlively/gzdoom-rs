// ZScript bytecode VM.
//
// The GZDoom VM is a register-based machine.  Each function has a fixed
// register file; opcodes operate on typed registers (int, float, pointer).
//
// See: src/common/scripting/vm/vmexec.h  (the interpreter loop)
//      src/common/scripting/vm/vmbuilder.h (bytecode builder)

// Stub — define the opcode enum and register file structure here first.
